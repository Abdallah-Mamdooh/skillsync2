[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = [
  "npm install",
  "python -m pip install --upgrade pip",
  "python -m pip install -r src/resume_analyzer/requirements.txt"
]

[start]
cmd = "npm start"